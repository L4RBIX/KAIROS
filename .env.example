# Live LightGBM inference API (FastAPI). Leave unset to use the demo mock.
VITE_ML_API_URL=http://localhost:8000

# Optional OSRM routing endpoint (default: public demo server)
# VITE_OSRM_URL=https://router.project-osrm.org

# DeepSeek keys belong in backend/.env only — never here.
